[project]
name = "forensic-checkpointctl"
version = "0.1.0"
description = "Heuristic forensic analysis for checkpointctl inspect JSON (CRIU container checkpoints)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
